services:
  postgres:
    image: pgvector/pgvector:pg16
    ports:
      - "${VERSA_DB_PORT:-5434}:5432"
    environment:
      POSTGRES_USER: versa
      POSTGRES_PASSWORD: versa
      POSTGRES_DB: versa
    volumes:
      - versa_pgdata:/var/lib/postgresql/data
volumes:
  versa_pgdata: